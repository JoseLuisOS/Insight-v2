# Log de sesión — 2026-06-27 — Fase 0: Setup

## Objetivo
Arrancar la ejecución del PLAN.md: cimientos del proyecto en cloud (Supabase + Vercel) y
estructura del repositorio con documentación, bitácora y aprendizajes.

## Cronología

1. Leído `docs/PLAN.md` completo (plan v1.0, ~460 líneas).
2. Verificado entorno local: Node v26, npm 11.12, git 2.50, Supabase CLI 2.101, Docker 29.
   `gh` autenticado como `arturodiazmo`.
3. Decisión del usuario: **todo en cloud** (no local). → Supabase hosted + Vercel.
   Supabase vía MCP; deploy vía GitHub + import en Vercel.
4. Listadas organizaciones Supabase: 1 org (`ioxyfptmrqcuetaotkua`), 3 proyectos existentes.
5. Confirmado costo ($10/mes) y creado proyecto **`intersel-insight`**
   (ref `kytvxyjvnxamqdrhwezw`, `us-east-1`).
6. Scaffold Next.js 16.2.9 (App Router, TS, Tailwind, ESLint, `src/`, alias `@/*`) en
   subcarpeta `intersel-insight/` (nombre válido npm; el dir padre tiene espacios).
7. Creada estructura de documentación: `CLAUDE.md`, `docs/{PLAN,LOG,APRENDIZAJES,DECISIONES}.md`,
   `logs/`, `.env.example`, `README.md`. Ajustado `.gitignore`.

## Decisiones registradas
D-001 (tenencia), D-002 (cloud), D-003 (estructura repo), D-004 (ECharts). Ver `docs/DECISIONES.md`.

## Pendiente
- Subir repo a GitHub e importar en Vercel.
- Migraciones: esquemas `app` + `tenant_data`, rol `app_readonly`, modelo de datos, RLS capa 1,
  access token hook, suite de tests de aislamiento.
- Auth + onboarding de tenant/usuario.

## Notas / gotchas
- Next 16 tiene breaking changes → leer `node_modules/next/dist/docs/` antes de codear (AGENTS.md).
- `create-next-app .` falla si el nombre del dir tiene mayúsculas/espacios → usar subcarpeta.
